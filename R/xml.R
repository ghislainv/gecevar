#' Soilgrids legend
#'
#' @description
#' Internal data.
#' Contains soilgrids legend for output files `gecevar.tif` and `environ.tif`.
#' If we want to open `gecevar.tif` or `environ.tif` with QGIS, we will need `RasterAttributeTable` extention to add soilgrid's legend.
#' This legend contains all type of soil available in [soilgrids.org](https://soilgrids.org/).
#'
"xml"
